# Raw spectrum feed for the pill rest visualizer (read by Singletons/Cava.qml).
[general]
mode = normal
framerate = 60
bars = 9
autosens = 1

[input]
method = pipewire
source = auto

[output]
method = raw
raw_target = /dev/stdout
data_format = ascii
ascii_max_range = 100
channels = mono
